[pytest]
testpaths = backend/tests
# Hiện tên từng ca kiểm thử trên terminal, tránh output chỉ gồm dấu chấm.
addopts = -ra -v -m "not integration"
markers =
    integration: kiểm thử tích hợp qua API Flask với ontology và dữ liệu mẫu.
